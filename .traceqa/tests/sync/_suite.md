---
description: Git synchronization, branch switching, webhook processing, and conflict resolution
icon: git-branch
tags: [sync, git, branches, webhooks]
---

# Git Sync & Branches

Covers the GitHub App webhook-driven sync pipeline, manual sync triggers, branch switching within workspaces, and handling of parse errors or sync conflicts.
